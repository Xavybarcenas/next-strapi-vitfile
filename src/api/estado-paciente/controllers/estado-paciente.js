'use strict';

/**
 * estado-paciente controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::estado-paciente.estado-paciente');
